@extends('layouts.app')

@section('body')
@php
    $primaryAdmin = strtolower(env('ADMIN_BOOTSTRAP_EMAIL', 'admin@empresa.com'));
    $hasAppPassword = !empty($settings?->app_password_hash);
    $input = 'w-full rounded-xl border border-slate-200 bg-slate-50 px-3.5 py-2.5 text-xs font-semibold text-slate-800 outline-none transition focus:border-indigo-400 focus:bg-white focus:ring-2 focus:ring-indigo-100';
@endphp
<div class="shell">@include('partials.sidebar')<main class="main">@include('partials.header',['title'=>'Configuración Corporativa'])
<div class="content max-w-5xl">
    <div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-center">
        <div><p class="mb-1 font-mono text-xs font-bold tracking-wider text-slate-400">CONFIGURACIÓN VISUAL INICIAL</p><p class="text-xs text-slate-500">Define horarios operativos, frecuencia del agente y el estado global del bloqueo web. Las listas de apps y dominios se administran desde Políticas de Uso.</p></div>
        <button type="submit" form="settings-form" class="inline-flex shrink-0 items-center justify-center gap-2 rounded-xl bg-indigo-600 px-5 py-2.5 font-mono text-xs font-bold text-white shadow-md shadow-indigo-100 transition hover:bg-indigo-500"><span aria-hidden="true">▣</span> Guardar cambios</button>
    </div>

    @if(session('success'))<div class="mb-6 flex items-center gap-2 rounded-xl border border-emerald-100 bg-emerald-50 p-4 text-xs font-semibold text-emerald-800"><span class="flex h-4 w-4 items-center justify-center rounded-full bg-emerald-500 text-[10px] text-white">✓</span>{{ session('success') }} Los cambios se aplicarán instantáneamente.</div>@endif
    @if($errors->any())<div class="mb-6 flex items-center gap-2 rounded-xl border border-red-100 bg-red-50 p-4 text-xs font-semibold text-red-800">⚠ {{ $errors->first() }}</div>@endif

    <form id="settings-form" method="post" action="{{ route('settings.save') }}" class="space-y-8">@csrf
        <section class="grid grid-cols-1 gap-6 rounded-2xl border border-slate-100 bg-white p-6 shadow-sm md:grid-cols-4">
            <label class="space-y-1.5 md:col-span-2"><span class="block font-mono text-[10px] font-bold text-slate-400">DATOS DE LA EMPRESA (NOMBRE)</span><div class="relative"><span class="absolute left-3.5 top-2.5 text-slate-400">▦</span><input class="{{ $input }} pl-10" name="company_name" required value="{{ old('company_name',$settings->company_name ?? config('worklive.company_name')) }}"></div></label>
            <label class="space-y-1.5"><span class="block font-mono text-[10px] font-bold text-slate-400">ZONA HORARIA CORPORATIVA</span><select class="{{ $input }}" name="timezone" required>@php($selectedTimezone = old('timezone', $settings->timezone ?? 'America/Mexico_City'))@foreach(['America/Mexico_City'=>'Ciudad de México (UTC-6)','America/Bogota'=>'Bogotá (UTC-5)','America/Lima'=>'Lima (UTC-5)','America/New_York'=>'Nueva York (UTC-5/-4)','America/Chicago'=>'Chicago (UTC-6/-5)','America/Los_Angeles'=>'Los Ángeles (UTC-8/-7)','America/Sao_Paulo'=>'São Paulo (UTC-3)','Europe/Madrid'=>'Madrid (UTC+1/+2)','UTC'=>'UTC'] as $timezone => $label)<option value="{{ $timezone }}" @selected($selectedTimezone === $timezone)>{{ $label }}</option>@endforeach</select></label>
            <label class="space-y-1.5"><span class="block font-mono text-[10px] font-bold text-slate-400">HORARIOS ADMINISTRATIVOS</span><span class="flex items-center gap-2"><input class="{{ $input }} min-w-0 px-1 text-center" maxlength="5" name="business_hours_start" required value="{{ old('business_hours_start',$settings->business_hours_start ?? '08:00') }}"><b class="text-xs text-slate-400">a</b><input class="{{ $input }} min-w-0 px-1 text-center" maxlength="5" name="business_hours_end" required value="{{ old('business_hours_end',$settings->business_hours_end ?? '18:00') }}"></span></label>
            <label class="space-y-1.5"><span class="block font-mono text-[10px] font-bold text-slate-400">RETARDO DESPUÉS DE</span><span class="flex items-center gap-2"><input class="{{ $input }} text-center" type="number" min="0" max="180" name="late_arrival_grace_minutes" value="{{ old('late_arrival_grace_minutes',$settings->late_arrival_grace_minutes ?? 10) }}"><b class="text-[10px] uppercase text-slate-400">min</b></span></label>
            <label class="space-y-1.5 md:col-start-4"><span class="block font-mono text-[10px] font-bold text-slate-400">SALIDA TEMPRANA SI FALTAN</span><span class="flex items-center gap-2"><input class="{{ $input }} text-center" type="number" min="0" max="180" name="early_departure_grace_minutes" value="{{ old('early_departure_grace_minutes',$settings->early_departure_grace_minutes ?? 10) }}"><b class="text-[10px] uppercase text-slate-400">min</b></span></label>
        </section>

        <section class="space-y-5 rounded-2xl border border-slate-100 bg-white p-6 shadow-sm"><h3 class="flex items-center gap-2 text-sm font-bold text-slate-900"><span class="text-slate-500">◷</span> Configuración del Agente Windows</h3>
            <div class="grid grid-cols-1 gap-6 md:grid-cols-3">
                <label class="space-y-1.5"><span class="block font-mono text-[10px] font-bold text-slate-400">MEDIR CADA (SEGUNDOS)</span><input class="{{ $input }} text-center" type="number" min="5" max="300" name="agent_sample_interval_seconds" value="{{ old('agent_sample_interval_seconds',$settings->agent_sample_interval_seconds ?? 15) }}"></label>
                <label class="space-y-1.5"><span class="block font-mono text-[10px] font-bold text-slate-400">ENVIAR DATOS CADA (SEGUNDOS)</span><input class="{{ $input }} text-center" type="number" min="30" max="3600" name="agent_batch_interval_seconds" value="{{ old('agent_batch_interval_seconds',$settings->agent_batch_interval_seconds ?? 300) }}"></label>
                <label class="space-y-1.5"><span class="block font-mono text-[10px] font-bold text-slate-400">CONSULTAR INSTRUCCIONES CADA (SEGUNDOS)</span><input class="{{ $input }} text-center" type="number" min="10" max="3600" name="agent_poll_interval_seconds" value="{{ old('agent_poll_interval_seconds',$settings->agent_poll_interval_seconds ?? 30) }}"><span class="block text-[10px] text-slate-400">Controla cuándo el cliente consulta solicitudes y cambios de política.</span></label>
                <label class="flex items-center justify-between gap-4 rounded-xl border border-slate-200 bg-slate-50 p-4"><span class="font-mono text-[10px] font-bold uppercase text-slate-500">Bloquear dominios no productivos</span><input class="h-5 w-5 accent-indigo-600" type="checkbox" name="block_websites_enabled" value="1" @checked(old('block_websites_enabled',$settings->block_websites_enabled ?? false))></label>
            </div>
            <label class="block rounded-xl border border-slate-200 bg-slate-50 p-4"><span class="block font-mono text-[10px] font-bold uppercase text-slate-500">Prefijo para KEY de vinculación</span><span class="mb-3 mt-1 block text-xs text-slate-500">Define con qué texto iniciarán las nuevas KEYs del cliente. Ejemplo: <b>SAFEB</b>-XXXX-XXXX-XXXX-XXXX.</span><input class="{{ $input }} bg-white font-mono font-black tracking-wider" name="client_key_prefix" maxlength="12" required value="{{ old('client_key_prefix',$settings->client_key_prefix ?? 'SAFEB') }}" oninput="this.value=this.value.toUpperCase().replace(/[^A-Z0-9]/g,'')"></label>
            <label class="block rounded-xl border border-slate-200 bg-slate-50 p-4"><span class="block font-mono text-[10px] font-bold uppercase text-slate-500">URL de actualizaciones del cliente</span><span class="mb-3 mt-1 block text-xs text-slate-500">URL pública al archivo <b>latest.json</b> que usará WorkLive Tracker al presionar “Buscar actualización”.</span><input class="{{ $input }} bg-white" type="url" name="update_feed_url" value="{{ old('update_feed_url',$settings->update_feed_url ?? '') }}" placeholder="https://tu-dominio.com/worklive-tracker/latest.json"></label>
            <div class="flex flex-col justify-between gap-4 rounded-xl border border-slate-200 bg-slate-50 p-4 md:flex-row md:items-center"><div class="max-w-xl"><span class="block font-mono text-[10px] font-bold uppercase text-slate-500">Contraseña para App Cliente</span><p class="mt-1 text-xs text-slate-500">Se solicitará para cerrar realmente el Tracker y para mostrar la ruta de archivos locales. {{ $hasAppPassword ? 'Ya existe una contraseña configurada.' : 'Aún no hay contraseña configurada.' }}</p></div><div class="relative w-full md:w-80"><input id="app-password" class="{{ $input }} bg-white pr-10" type="password" name="app_password" minlength="8" placeholder="{{ $hasAppPassword ? 'Nueva contraseña opcional' : 'Crear contraseña' }}"><button class="absolute right-2 top-1/2 -translate-y-1/2 p-1 text-slate-400" type="button" data-password-toggle="app-password" aria-label="Mostrar contraseña">◉</button></div></div>
            <div class="hidden"><textarea name="productive_apps_list">{{ old('productive_apps_list',$settings->productive_apps_list ?? '') }}</textarea><textarea name="unproductive_apps_list">{{ old('unproductive_apps_list',$settings->unproductive_apps_list ?? '') }}</textarea><textarea name="productive_domains_list">{{ old('productive_domains_list',$settings->productive_domains_list ?? '') }}</textarea><textarea name="unproductive_domains_list">{{ old('unproductive_domains_list',$settings->unproductive_domains_list ?? '') }}</textarea></div>
        </section>
    </form>

    <section class="space-y-4"><h3 class="flex items-center gap-2 text-sm font-bold text-slate-900"><span class="text-slate-500">◇</span> Administradores Autorizados con Acceso al Panel</h3><div class="space-y-6 rounded-2xl border border-slate-100 bg-white p-6 shadow-sm"><p class="max-w-2xl text-xs leading-relaxed text-slate-500">Solo las cuentas listadas podrán autenticarse en el panel administrativo de <b>WorkLive Pro</b>. El administrador senior predeterminado no puede ser removido para evitar bloqueos del sistema.</p><div class="grid grid-cols-1 gap-8 md:grid-cols-12"><div class="space-y-4 md:col-span-5 md:border-r md:border-slate-100 md:pr-8"><span class="block font-mono text-[10px] font-bold uppercase tracking-wider text-slate-400">DAR DE ALTA NUEVO ADMINISTRADOR</span><form method="post" action="{{ route('settings.admins.store') }}" class="space-y-3">@csrf<label class="block space-y-1.5"><span class="block text-[10px] font-bold uppercase text-slate-400">Correo Electrónico Google</span><input class="{{ $input }}" type="email" name="email" required placeholder="ejemplo@gmail.com"></label><label class="block space-y-1.5"><span class="block text-[10px] font-bold uppercase text-slate-400">Contraseña</span><span class="relative block"><input id="new-admin-password" class="{{ $input }} pr-10" type="password" name="password" minlength="8" required placeholder="Mínimo 8 caracteres"><button class="absolute right-2 top-1/2 -translate-y-1/2 p-1 text-slate-400" type="button" data-password-toggle="new-admin-password" aria-label="Mostrar contraseña">◉</button></span></label><button class="flex w-full items-center justify-center gap-2 rounded-xl bg-indigo-600 py-2.5 font-mono text-xs font-bold text-white shadow transition hover:bg-indigo-500">＋ Dar de Alta</button></form></div><div class="space-y-4 md:col-span-7"><span class="block font-mono text-[10px] font-bold uppercase tracking-wider text-slate-400">CUENTAS ACTIVAS CON CLAVE DE AUDITOR</span><div class="overflow-hidden rounded-xl border border-slate-100 bg-slate-50/30 divide-y divide-slate-100">@forelse($admins as $admin)@php($isSenior = strtolower($admin->email) === $primaryAdmin)<div class="flex items-center justify-between gap-3 p-3.5 text-xs"><div><div class="flex items-center gap-2"><span class="font-semibold text-slate-800">{{ $admin->email }}</span><span class="rounded px-2 py-0.5 font-mono text-[9px] font-black uppercase {{ $isSenior ? 'bg-indigo-100 text-indigo-800' : 'bg-slate-100 text-slate-600' }}">{{ $isSenior ? 'Senior Admin' : 'Asignado' }}</span></div><span class="mt-0.5 block font-mono text-[10px] text-slate-400">Dado de alta por: {{ $admin->added_by }} · {{ \Carbon\Carbon::parse($admin->added_at)->format('d/m/Y') }}</span><span class="block font-mono text-[10px] text-slate-400">Password: {{ $admin->password_hash ? 'Configurada' : 'Pendiente' }}</span></div>@unless($isSenior)<form method="post" action="{{ route('settings.admins.delete',$admin->email) }}" onsubmit="return confirm('¿Retirar permisos de administrador?')">@csrf @method('DELETE')<button class="rounded-lg p-2 text-slate-400 transition hover:bg-red-50 hover:text-red-600" title="Eliminar administrador">⌫</button></form>@endunless</div>@empty<div class="p-4 text-xs text-slate-400">No hay administradores registrados.</div>@endforelse</div></div></div></div></section>
</div></main></div>
<script>document.querySelectorAll('[data-password-toggle]').forEach((button)=>button.addEventListener('click',()=>{const input=document.getElementById(button.dataset.passwordToggle);input.type=input.type==='password'?'text':'password';button.textContent=input.type==='password'?'◉':'◌';}));</script>
@endsection
